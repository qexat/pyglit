from setuptools import find_packages, setup

if __name__ == "__main__":
    setup(
        name="pyglit",
        version="0.1.0",
        description="Iterators inspired from Gleam",
        author="Qexat",
        packages=find_packages(),
    )
